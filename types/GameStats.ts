interface GameStats {
  totalPRs: number;
  totalComments: number;
  totalCommits: number;
}

export default GameStats;
export interface GameStats {
  totalPRs: number;
  totalComments: number;
  totalCommits: number;
}
